# 项目决策记忆（由 memagent 自动生成）

生成时间：2026-08-14 17:40；
来源：记忆层（共 27 条决策）。
本文件供支持 AGENTS.md / CLAUDE.md 风格的 agent 在会话开始时加载。
重新运行 `python session_memory.py --export-agents-md` 可同时刷新
AGENTS.md 与 CLAUDE.md（内容同步）。

## semantic — 项目事实与决策（语义类：稳定知识，被反复确认的越靠前）

- 开发决策：记忆层嵌入三种模式：prompt 注入（remember_agent）/ 外部记忆工具（session_memory）/ 指令文件生成器（--export-agents-md）（重要 0.45 · 检索 0 次）
- 开发决策：再巩固可塑性 = 1 − importance：重要性越高越稳定，importance ≥ 0.8 完全冻结（核心记忆）（重要 0.20 · 检索 0 次）
- 开发决策：语义化评分从观测历史推导（相邻快照 access_count 增大 = 一次使用），无需额外状态（重要 0.20 · 检索 0 次）
- 开发决策：access_count ≥ 2 守护：从未被使用过的新事实不会一觉醒来翻转成情景（重要 0.20 · 检索 0 次）
- 开发决策：查询同义扩展：人称互换（您→我）+ 同义词替换（用餐→吃），原始查询恒在变体首位，rel 只升不降（重要 0.20 · 检索 0 次）
- 开发决策：遗忘斜率对比用触底时间而非每 τ 斜率比：斜率归一化后纯 recency 衰减跨类型相同，且触底钳制会扭曲斜率比（重要 0.20 · 检索 0 次）
- 开发决策：触底验证：观测采样按真实 τ（true_tau_by_type）记录，历史里首个强度 ≤ 0.2 的采样点 = 实测触底时刻（重要 0.20 · 检索 0 次）
- 开发决策：贴合度 fit = 1 − |实测τ − 配置τ| / 配置τ；τ 失准靠 true_tau_by_type 模拟隐藏真实遗忘来暴露（重要 0.20 · 检索 0 次）
- 开发决策：对照实验靠可注入时钟 now_fn 确定性快进（秒级参数代表数天），不依赖真实 sleep、可精确复现（重要 0.20 · 检索 0 次）
- 开发决策：零检索对照组的内容与所有查询零 n-gram 共享，避免哈希嵌入泛化命中污染对照（重要 0.20 · 检索 0 次）
- 开发决策：循环导入坑：profiles ↔ visualize 互相依赖，用函数级导入（运行时再 import）解决（重要 0.20 · 检索 0 次）
- 开发决策：无 key 降级：记忆层独立于 LLM——检索/沉淀照常，只缺 LLM 回复（重要 0.20 · 检索 0 次）
- 开发决策：sync 闭环验证：git log 提炼→沉淀→导出→评估一条命令完成（重要 0.20 · 检索 0 次）
- 开发决策：开发决策：兴趣、图谱、成长、认知、探索等运行状态统一持久化到 store.meta.agent_state，并向后兼容旧 JSON。（重要 0.14 · 检索 0 次）
- 开发决策：开发决策：v0.2 持久化采用同目录原子替换、.bak 备份与陈旧写入检测，禁止并发进程静默覆盖数据。（重要 0.12 · 检索 0 次）
- 开发决策：开发决策：自主写作与 FoxTable 后台任务采用单实例锁、有限默认轮数和连续失败熔断。（重要 0.12 · 检索 0 次）
- 开发决策：小说正式主线只认作品 chapters 目录；定名前且章号重叠、时间线不接续的内容作为 pre-title draft 永久归档，不强行并入主线。（重要 0.12 · 检索 0 次）
- 开发决策：语义化双阈值滞回（3.0 / 0.8）避免评分在阈值附近来回振荡（重要 0.10 · 检索 0 次）

## skill — 经验与方法（技能类：被反复验证的做法）

- 开发决策：遗忘曲线学习器 learn_tau：EMA + 置信度门控，触底段（强度到 0.2 下限）不参与反推，避免无衰减信息污染估计（重要 0.20 · 检索 0 次）
- 开发决策：可塑性学习器 learn_plasticity 用中位数聚合实测因子：抗离群事件，极端相关性的回忆不会带偏估计（重要 0.20 · 检索 0 次）
- 开发决策：学习器门控：观测/事件不足、偏差过小（τ<0.5%、因子<1%）不更新，避免对稀疏数据过度反应（重要 0.20 · 检索 0 次）
- 开发决策：学习结果持久化到 store.meta 重启自动应用；因子只落盘显式配置过的通道，未学习类型保持默认（重要 0.20 · 检索 0 次）
- 开发决策：按类型再巩固因子：技能 drift 0.15（回忆时高度稳定）、语义 1.0 基准、情景 2.5（易被情境改写）（重要 0.20 · 检索 0 次）
- 开发决策：技能类一致性校验钩子完全短路：内容/向量/修订全不动，技能回忆是验证不是吸收情境；仅冲突修正才计修订（重要 0.20 · 检索 0 次）
- 开发决策：章节写作按作品串行化并拒绝覆盖；正文低于目标字数 90% 或没有完整句末时视为截断且不落盘。（重要 0.12 · 检索 0 次）
- 开发决策：作品保护通过 work_admin 维护章节哈希、全书快照、覆盖证据、候选稿与逐章修订档案，修复稿审核后再 promote。（重要 0.12 · 检索 0 次）

## episodic — 历史事件（情景类：随遗忘可能过时）

- 开发决策：哈希 n-gram 嵌入对措辞敏感：短查询/泛化命中会误伤无关记忆（火锅碰撞首都），实验与演示要用完整句子（重要 0.20 · 检索 0 次）

<!-- 由 memagent 记忆层自动生成，重新运行 --export-agents-md 更新 -->